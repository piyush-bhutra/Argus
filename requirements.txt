# Lower bounds are security floors, not preferences. `fastapi>=0.100.0` let a
# fresh Docker build resolve starlette 0.52.x, which carries several advisories
# (PYSEC-2026-161/248/249/2280/2281) in the HTTP layer that serves every
# request. Do not relax these without re-running: python -m pip_audit
#
# Upper bounds pin the major version so a build cannot silently cross a
# breaking release. Verified together against the full test suite.

fastapi>=0.121.0,<1.0.0
starlette>=1.3.1            # transitive via fastapi; floored explicitly
uvicorn>=0.41.0,<1.0.0
pydantic>=2.12.0,<3.0.0
pydantic-settings>=2.0.0,<3.0.0
networkx>=3.0,<4.0
rank_bm25>=0.2.2,<1.0
scikit-learn>=1.3.0,<2.0
matplotlib>=3.7.0,<4.0
openai>=1.0.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0

# Offline evaluation only - not imported by app/, so not needed to serve the API.
datasets>=2.14.0,<6.0.0

# Test and audit tooling.
pytest>=7.4.0,<9.0.0
pip-audit>=2.7.0
